{
  "name": "seatify",
  "private": true,
  "workspaces": [
    "Seatify-React-Frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=Seatify-React-Frontend",
    "build": "npm run build --workspace=Seatify-React-Frontend",
    "dev:backend": "dotnet run --project backend-dotnet/src/Seatify.Api",
    "dev:full": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev\"",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
